require "rails_helper"

RSpec.describe "invoicing", type: :system do
    context "creating invoices" do
        it "shows unbilled billable hours for a client" do

        end

        it "generates an .html invoice" do

        end

        it "created invoice contains the relevant data" do

        end

        it "sends the invoice by email to the client" do

        end
    end
end
