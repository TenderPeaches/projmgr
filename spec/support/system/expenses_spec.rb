require "rails_helper"

RSpec.describe "expenses", type: :system do
    context "creating new expenses" do
        it "can select from a list of expense categories" do

        end

        it "creates the expense when submitting the form" do

        end

        it "shows the new expense form again after successful expense creation" do

        end
    end

    context "viewing expenses" do
        it "shows a yearly breakdown" do

        end

        it "shows the total expenses for each category" do

        end
    end
end
