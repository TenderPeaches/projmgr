require "rails_helper"

##
# projects CRUD
RSpec.describe "project management", type: :system do
    context "creating a new project" do
        it "can select a client or create a new one" do

        end

        it "creates the project when submitting the form" do

        end

        it "redirects to the project page" do

        end
    end

    context "modifying an existing project" do
        it "updates the project" do

        end

        it "destroys the project" do

        end
    end
end
