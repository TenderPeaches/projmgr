require "rails_helper"

RSpec.describe "timekeeping", type: :system do
    context "entering hours" do
        it "shows a list of projects for which time is entered" do

        end

        it "creates a shift when the form is submitted" do

        end
    end

    context "viewing hours" do
        it "shows a list of billable hours per project" do

        end

        it "can filter by shift type" do

        end

        it "shows inferred data" do

        end
    end
end
