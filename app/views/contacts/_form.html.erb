<%= model_form_with(model: contact, class: 'form', url: contacts_path(client_id: options[:client_id] || nil)) do |form| %>
    <%= render "contacts/fields", form: form %>

    <%= tag.div class: "actions" do %>
        <%= form.submit %>
    <% end %>
<% end %>
