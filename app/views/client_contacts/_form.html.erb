<%= form_with(model: client_contact) do |form| %>
  <% if client_contact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(client_contact.errors.count, "error") %> prohibited this client_contact from being saved:</h2>

      <ul>
        <% client_contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :contact_id, style: "display: block" %>
    <%= form.text_field :contact_id %>
  </div>

  <div>
    <%= form.label :client_id, style: "display: block" %>
    <%= form.text_field :client_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
