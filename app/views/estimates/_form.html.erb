<%= form_with(model: estimate) do |form| %>
  <% if estimate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(estimate.errors.count, "error") %> prohibited this estimate from being saved:</h2>

      <ul>
        <% estimate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :statement_id, style: "display: block" %>
    <%= form.text_field :statement_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
