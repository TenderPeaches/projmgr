<%= turbo_stream.update :title, model.to_s %>

<%= turbo_stream.update :form do %>
  <%# render "clients/form, client: @client" line %>
  <%= render "#{model.class.model_name.route_key}/form", model.class.model_name.singular.to_sym => model %>
<% end %>
