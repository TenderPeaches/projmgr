

<%= tag.h1 "Products" %>

<%= tag.div id: "products" do %>
  <% @products.each do |product| %>
    <%= render product %>
    <p>
      <%= link_to "Show this product", product %>
    </p>
  <% end %>
<% end %>

<%= link_to "New product", new_product_path %>
