

<%= tag.h1 "Tasks" %>

<%= tag.div id: "tasks" do %>
  <% @tasks.each do |task| %>
    <%= render task %>
    <p>
      <%= link_to "Show this task", task %>
    </p>
  <% end %>
<% end %>

<%= link_to "New task", new_task_path %>
