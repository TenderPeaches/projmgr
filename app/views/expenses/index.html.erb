

<%= tag.h1 "Expenses" %>

<%= render partial: "expenses/list", locals: { expenses: @expenses } %>
<%= tag.div id: "new-expenses" %>

<%= link_to t('actions.new', thing: t('models.expense.model_name')), new_expense_path, class: "button", data: { turbo_stream: true } %>
