

<%= tag.h1 "Events" %>

<%= tag.div id: "events" do %>
  <% @events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to "Show this event", event %>
    </p>
  <% end %>
<% end %>

<%= link_to "New event", new_event_path %>
